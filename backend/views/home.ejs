<html>
<head>
  <%- include('partials/head.ejs') %>
</head>
<body>
  <h1>Home Page (Authenticated) - Hi <%= user.username %> (id: <%= user.id %>)</h1>
  <%- include('partials/authenticated-nav.ejs') %>
    <div class="container">
      <div class="row">
        <div class="col-sm">
           <!-- <form method="get" action="/api/games/create">
            <button type="submit" class="ms-btn ms-action" value="Create Game">Create Game</button> 
            removed because hitting enter on chat creates game  -->
            <a
            href="/api/games/create"
            id="create-game-link"
            class="ms-btn ms-action"
            >Create Game</a
          >
            <h2>Available Games</h2>
            <ul id="game-list">
              <% for(let i=0; i < games.length; i++) { %>
                <li>
                  <a href="/api/games/<%= games[i].id %>/join">Join <span>
                      <%= games[i].id %>
                    </span></a>
                </li>
                <% } %>
            </ul>
        </div>
        <div class = "chat" style="border: solid;">
        <%- include('partials/chat.ejs') %>
        </div>
      </div>
    </div>
        </div>
      </div>
    </div>
    <template id="available-game-item">
      <div>
        <li>
          <a href="#">Join <span>42</span></a>
        </li>
      </div>
    </template>
</body>
</html>